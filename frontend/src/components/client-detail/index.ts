/**
 * Client Detail Components
 *
 * Components for the client detail page including navigation and layouts.
 */

// Main Ledger Cards Header (active design)
export {
  LedgerCardsHeader,
  StatCard,
  QualityMeter,
  InsightRow,
  MetricPill,
  type Tab,
} from './LedgerCardsHeader';

// Traffic Light Dashboard (Option B - primary dashboard)
export { TrafficLightDashboard } from './TrafficLightDashboard';

// Legacy navigation component (deprecated)
export { ClientNavigation } from './ClientNavigation';

// Alternative design components (for reference/future use)
// export { ClientDetailRedesign } from './ClientDetailRedesign';
// export { ClientDetailCompact } from './ClientDetailCompact';
